pipeline {
    agent any
    //agent { docker { image 'golang' } }
 	  // Clean workspace before doing anything
    //deleteDir()
    stage ('Start') {
      steps {
        sh 'pwd'
      }
    }
  }
